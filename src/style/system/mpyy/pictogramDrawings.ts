import type { PictogramName } from './pictograms';

/**
 * The pictogram drawings (SVG, conventions in ./pictograms.ts). A name
 * without a drawing here shows the placeholder.
 */
export const DRAWN: Partial<Record<PictogramName, string>> = {};
