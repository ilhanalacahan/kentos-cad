{
  "name": "kentos-cad",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "node scripts/e2e/smoke.mjs",
    "rust:test": "cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings",
    "rust:wasm": "cargo build -p kentos-wasm --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-dir src/wasm/pkg target/wasm32-unknown-unknown/release/kentos_wasm.wasm",
    "test:rust": "pnpm rust:test && pnpm rust:wasm && vitest run src/wasm"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
